# VECTRA-X — Python dependencies
# Tested with Python 3.12 on Windows 11. Version lower-bounds reflect the
# environment the pipeline was developed and verified on; newer patch releases
# are expected to work. Install with:  pip install -r requirements.txt

# --- core data / ML ---
pandas>=2.2          # tested 3.0.2
numpy>=1.26          # tested 2.4.4
scikit-learn>=1.4    # tested 1.8.0
scipy>=1.11          # tested 1.17.1
joblib>=1.3          # tested 1.5.3

# --- multi-label stratification ---
iterative-stratification>=0.1.9

# --- gradient boosting engines (optional but recommended; pipeline falls back
#     to sklearn HistGradientBoosting if these are missing) ---
xgboost>=2.0         # tested 3.2.0
lightgbm>=4.0        # tested 4.6.0

# --- visualisation ---
matplotlib>=3.7      # tested 3.11.0
seaborn>=0.13        # tested 0.13.2
plotly>=6.0          # interactive notebook and Streamlit charts

# --- explainability (optional; permutation importance is the always-on fallback) ---
shap>=0.44           # tested 0.52.0

# --- dashboard ---
streamlit>=1.30      # tested 1.58.0

# --- final competition notebook ---
nbformat>=5.10
nbclient>=0.10
ipykernel>=6.29

# --- IO / config ---
openpyxl>=3.1        # read the data dictionary .xlsx
PyYAML>=6.0          # config.yaml
pypdf>=4.0           # optional: re-extract the blueprint PDF text
